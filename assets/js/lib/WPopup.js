// Generated by CoffeeScript 1.7.1
(function() {
  var $,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  $ = jQuery;

  this.WPopup = (function() {
    var config;

    function WPopup() {
      this.notice = __bind(this.notice, this);
    }

    config = {
      timeout: 1500,
      opacity: 0.85
    };

    WPopup.prototype.html = function(data, height, width) {
      if (height == null) {
        height = 200;
      }
      if (width == null) {
        width = 600;
      }
      return $.colorbox({
        html: data,
        innerWidth: width,
        innerHeight: height,
        transition: 'none',
        opacity: 0.52
      });
    };

    WPopup.prototype.iframe = function(url, height, width) {
      if (height == null) {
        height = 200;
      }
      if (width == null) {
        width = 600;
      }
      return $.colorbox({
        iframe: true,
        href: url,
        innerWidth: width,
        innerHeight: height,
        transition: 'none'
      });
    };

    WPopup.prototype.notice = function(message, type, height, width) {
      var msg;
      if (type == null) {
        type = "notic";
      }
      if (height == null) {
        height = 200;
      }
      if (width == null) {
        width = 600;
      }
      msg = $('<div></div>').addClass('text-center').addClass(type).html(message);
      return WPopup.prototype.html(msg, height, width);
    };

    WPopup.prototype.close = function() {
      return $.colorbox.close();
    };

    WPopup.prototype.auto_close = function(time) {
      if (time == null) {
        time = config.timeout;
      }
      return setTimeout(function() {
        return WPopup.prototype.close();
      }, time);
    };

    return WPopup;

  })();

}).call(this);

//# sourceMappingURL=WPopup.map
